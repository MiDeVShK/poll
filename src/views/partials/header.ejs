<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="description" content="Poll est un site de sondage à deux choix où les utilisateurs peuvent voter et créer des sondages.">
        <meta name="keywords" content="sondage, vote, deux choix, créer, utilisateur">
        <meta name="author" content="Hugo Laurent, Romain Leconte, Michael Dutheil ">
        <meta name="robots" content="index, follow">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta property="og:title" content="Poll - Make Your Choice">
        <meta property="og:description" content="Poll est un site de sondage à deux choix où les utilisateurs peuvent voter et créer des sondages.">
        <meta property="og:image" content="https://laurenthugo.com/images/poll.png">
        <meta property="og:url" content="https://www.my-poll.fr/">
        <link rel="stylesheet" href="/css/style.css?<%= Math.random().toString(36).substr(2, 9) %>">
        <link rel="stylesheet" href="/css/darkStyle.css?<%= Math.random().toString(36).substr(2, 9) %>">
        <title>Poll's</title>
        <script defer src="/js/app.js"></script>
    </head>
<body>
    
    <header>
        <!-- <h1 class="header-title"><a href="/">Poll's</a></h1> -->
        
            <a href="/"><img class="header-images-logo" src="/images/logo.png" alt=""></a>
            <!-- <img class="header-images-burger" src="/images/burger.png" alt=""> -->
            <% if (locals.errorMessage) { %>                
                <p style="color:red; font-size:30px;"> <%= errorMessage %></p>
           
            <% } else if (locals.succesMessage) { %>                
                <p style="color: green; font-size:30px;"> <%= succesMessage %></p>
            <% } %>
           <!-- faut laisser else if sinon ca va bug ok ty bebou, je test --> 
            

        <div class="navbar-responsive">
            <ul class="header-navbar">
                <li class="navbar-button home-button"><a href="/">Home</a></li>
                <li class="navbar-button"><a href="/polls">Polls</a></li>
                <% if (locals.user && locals.user.role === "member" ) { %>
                    <li class="navbar-button"><a class="navbar-admin" href="/member/<%= userId %>">Profil</a></li>
                  <% } %>               

                <% if (locals.user && locals.user.role === "admin") { %>
                    <li class="navbar-button"><a class="navbar-admin" href="/dashboard">Admin</a></li>
                    <li class="navbar-button"><a class="navbar-admin" href="/member/<%= userId %>">Profil</a></li>
                  <% } %>

                <% if (locals.user) { %>
                    <li class="navbar-button"><a class="navbar-logout" href="/logout">Log out</a></li>
                <% } else { %>
                    <li class="navbar-button signup-responsive"><a class="navbar-sign">Sign up</a></li>
                    <li class="navbar-button"><a class="navbar-connect">Connect</a></li>
                <% } %>

                <form action="" method="POST">
                    <li class="header-navbar-switch-container">
                        <input class="dark-theme-switch" type="checkbox" id="switch"/>
                        <label class="label-switch" for="switch"></label>
                    </li>
                </form> 
            </ul>
        </div>
        
    </header>
